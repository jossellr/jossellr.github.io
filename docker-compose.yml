services:
  web:
    image: nginx:alpine
    container_name: jossellr-web
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./.docker/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
